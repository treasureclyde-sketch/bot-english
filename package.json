{
  "name": "assistant-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute assistant-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute assistant-db --local --file=./schema.sql",
    "test": "node test/test_logic.mjs && node --experimental-sqlite test/test_db.mjs && node --experimental-sqlite test/test_assistant.mjs && node --experimental-sqlite test/test_callback.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
