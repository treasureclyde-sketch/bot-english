{
  "name": "learning-bot-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute learning-bot-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute learning-bot-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
